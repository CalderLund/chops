services:
  guitar-teacher:
    build: .
    ports:
      - "${CHOPS_PORT:-3847}:3000"
    volumes:
      # Persist practice data
      - ~/.guitar-teacher:/root/.guitar-teacher
    environment:
      - CHOPS_PORT=3000
    restart: unless-stopped
